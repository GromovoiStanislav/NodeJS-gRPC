## Error handling in gRPC (JS)

This example demonstrates basic RPC error handling in gRPC for unary and streaming response cardinalities.

```
node server -- --port 8080
node client -- --target localhost:8080
```
