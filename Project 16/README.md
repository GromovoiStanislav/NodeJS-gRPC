## gRPC with metadata and trailers "echo" example (JS)

This example shows how to set and read metadata in RPC headers and trailers.

```
node server --port 8080
node client --target localhost:8080
```
